* create directory structure
    /Domain
        /Articles
            /Exceptions
            /Rules
            /ValueObjects
            RepositoryInterface
        /Core
            CommandInterface
            EventInterface
    /Application
        /Articles
            /Commands
            /Events
            /Repositories
        /Core
            CommandBusInterface
            EventDispatcherInterface
    /Infrastructure
        /Http
            /Controllers
            /Middleware
        /Console
            /Commands
        /Core
            CommandBus
            EventDispatcher

* unit tests for LoginHandler, RateLimitedLoginHandler and LogoutHandler
* translations (Lang::get(), trans() or __()?)
* first admin page
* event bus
* query bus?
* security headers
* basic error pages
* add meta tags
* add microdata
* add scripts (composer/bash/docker) to automate tasks (install, update, test)
* automated backup
* fix readme
