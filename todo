* create directory structure
    /Domain
        /Blog
            /Exceptions
            /Rules
            /ValueObjects
            RepositoryInterface
        CommandInterface
        EventInterface
    /Application
        /Commands
        /Events
        /Repositories
        CommandBusInterface
        EventDispatcherInterface
    /Infrastructure
        /Http
            /Controllers
            /Middleware
        /Console
            /Commands
* seed real blogs (remove HtmlFaker)
* phpcs
* basic error pages
* unit test
* dusk test
* travis
* update dependencies
* security headers
* add meta tags
* add microdata
* add scripts (composer/bash/docker) to automate tasks (install, update, test)